@model Zamify.Models.Teacher
@{
    ViewData["Title"] = "Edit Teacher";
    Layout = null;
}

<div class="container py-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h3 class="card-title mb-0">Edit Teacher</h3>
        </div>
        <div class="card-body">
            <form asp-action="Edit" id="teacherEditForm">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                <input type="hidden" asp-for="Id" />

                <div class="row mb-4">
                    <div class="col-md-6">
                        <div class="form-floating mb-3">
                            <input asp-for="Name" class="form-control" placeholder="Name" />
                            <label asp-for="Name"></label>
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Email" class="form-control" placeholder="Email" type="email" />
                            <label asp-for="Email"></label>
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Password" class="form-control" placeholder="Password" type="password" />
                            <label asp-for="Password"></label>
                            <span asp-validation-for="Password" class="text-danger"></span>
                            <div class="form-text">Leave blank to keep current password.</div>
                        </div>

                      @*   <div class="form-floating mb-3">
                            <input asp-for="CreatedDate" class="form-control" placeholder="Created Date" type="date" readonly />
                            <label asp-for="CreatedDate"></label>
                            <span asp-validation-for="CreatedDate" class="text-danger"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="UpdatedDate" class="form-control" placeholder="Updated Date" type="date" />
                            <label asp-for="UpdatedDate"></label>
                            <span asp-validation-for="UpdatedDate" class="text-danger"></span>
                        </div> *@


                        <div class="form-floating mb-3">
                            <input asp-for="CreatedDate" class="form-control" readonly />
                            <label asp-for="CreatedDate"></label>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="UpdatedDate" class="form-control" readonly />
                            <label asp-for="UpdatedDate"></label>
                        </div>


                    </div>

                    <div class="col-md-6 d-flex align-items-center justify-content-center">
                        <div class="text-center p-4">
                            <div class="avatar-placeholder mb-3">
                                <i class="bi bi-person-circle"></i>
                            </div>
                            <p class="text-muted">Update teacher information while maintaining their existing records.</p>
                            <div class="alert alert-info" role="alert">
                                <i class="bi bi-info-circle"></i> Created on: @Model.CreatedDate.ToString("MMMM d, yyyy")
                            </div>
                        </div>
                    </div>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Back to List
                    </a>
                    <div>
                        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-info me-2">
                            <i class="bi bi-info-circle"></i> Details
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-save"></i> Save Changes
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@if (Layout == null)
{
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        :root {
            --primary-color: #3498db;
            --primary-hover: #2980b9;
            --secondary-color: #f8f9fa;
            --text-color: #2c3e50;
            --light-gray: #ecf0f1;
            --danger-color: #e74c3c;
            --info-color: #3498db;
        }

        body {
            background-color: var(--secondary-color);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--text-color);
        }

        .container {
            max-width: 900px;
        }

        .card {
            border-radius: 10px;
            border: none;
            overflow: hidden;
        }

        .card-header {
            background-color: var(--primary-color);
            padding: 1.25rem 1.5rem;
        }

        .card-body {
            padding: 2rem;
        }

        .form-floating > label {
            color: #6c757d;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
        }

        .form-control[readonly] {
            background-color: #f8f9fa;
        }

        .text-danger {
            color: var(--danger-color) !important;
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }

        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            padding: 0.5rem 1.5rem;
        }

            .btn-primary:hover {
                background-color: var(--primary-hover);
                border-color: var(--primary-hover);
            }

        .btn-outline-secondary {
            color: var(--text-color);
            border-color: #ced4da;
            padding: 0.5rem 1.5rem;
        }

            .btn-outline-secondary:hover {
                background-color: var(--light-gray);
                color: var(--text-color);
            }

        .btn-outline-info {
            color: var(--info-color);
            border-color: var(--info-color);
            padding: 0.5rem 1.5rem;
        }

            .btn-outline-info:hover {
                background-color: var(--info-color);
                color: white;
            }

        .avatar-placeholder {
            width: 120px;
            height: 120px;
            line-height: 120px;
            font-size: 60px;
            background-color: var(--light-gray);
            border-radius: 50%;
            color: #95a5a6;
            display: inline-block;
        }

        .form-text {
            color: #7f8c8d;
        }

        .alert {
            border-radius: 6px;
            padding: 0.75rem 1rem;
            margin-bottom: 1.5rem;
        }

        .alert-info {
            background-color: rgba(52, 152, 219, 0.1);
            border-color: rgba(52, 152, 219, 0.2);
            color: #2980b9;
        }
    </style>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Set today's date as default for UpdatedDate if empty
        document.addEventListener('DOMContentLoaded', function() {
            const updatedDateField = document.querySelector('input[asp-for="UpdatedDate"]');
            if (!updatedDateField.value) {
                const today = new Date().toISOString().split('T')[0];
                updatedDateField.value = today;
            }
        });
    </script>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        // Set default dates on client side
        document.addEventListener('DOMContentLoaded', function() {
            const dateFields = document.querySelectorAll('input[type="date"]');
            dateFields.forEach(field => {
                if(!field.value) {
                    field.value = new Date().toISOString().split('T')[0];
                }
            });
        });
    </script>

}