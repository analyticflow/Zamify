

@model Zamify.Models.Subject
@{
    ViewData["Title"] = "Edit Subject";
    Layout = "~/Views/Shared/_TeacherSidebar.cshtml";

}

<div class="page-header">
    <h1>Edit Subject</h1>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="form-container">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="Name" class="form-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="CreatedDate" class="form-label"></label>
                    <input asp-for="CreatedDate" class="form-control bg-light" readonly />
                </div>

                <div class="mb-3">
                    <label asp-for="UpdatedDate" class="form-label"></label>
                    <input asp-for="UpdatedDate" class="form-control bg-light" readonly />
                </div>

                <div class="d-flex gap-2 mt-4">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save me-1"></i>Save
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left me-1"></i>Back to List
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        // Show pop-up for duplicates
        $(document).ready(function () {
            $('form').submit(function (e) {
                var name = $('#Name').val().trim();
                var currentId = $('#Id').val() || 0;

                // Check duplicates via AJAX
                $.ajax({
                    url: '/Subjects/VerifySubject',
                    data: { name: name, id: currentId },
                    async: false
                }).done(function (response) {
                    if (response !== true) {
                        alert(response); // Show pop-up
                        e.preventDefault(); // Block form submission
                    }
                });
            });
        });
    </script>
}
